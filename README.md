# genreliser
Genres for your music. Under construction.

## Installation:
```bash
python3 -m venv venv
./venv/bin-activate
pip install -U pip
pip install -r requirements.txt
pip install -e .
mkdir -p logs data
```

## Usage:
```bash
genrelise -h
```
